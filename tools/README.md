# Tooling

Developer-facing utilities for the data-mining pipeline.

Planned tools:

- scan-game.py — scan an installed release
- extract-assets.py — extract supported Unity/raw data
- build-database.py — normalize extracted observations
- diff-releases.py — compare release snapshots

Tools should remain read-only against the game installation.
